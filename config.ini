
[config1]
model_name = Qwen/Qwen2.5-Coder-3B-Instruct
dtype = torch.float16
device_map = auto
max_new_tokens = 256
do_sample = True
use_cache = True
content_system_prompt = Return only valid Python Polars code. No markdown fences. Assign the final Polars DataFrame to result. Available datasets:


[config2]
model_name = Qwen/Qwen2.5-Coder-3B-Instruct
dtype = torch.float16
device_map = auto
max_new_tokens = 3000
do_sample = True
use_cache = True
content_system_prompt = Return only valid Python Polars code. No markdown fences. Assign the final Polars DataFrame to result. Available datasets:

[config3]
model_name = mistralai/Mistral-7B-Instruct-v0.2
dtype = torch.float32
device_map = auto
max_new_tokens = 512
do_sample = False
use_cache = True
content_system_prompt = Return only valid Python Polars code. 
    No markdown fences. Do not include any explanations or additional text, just the python code. 
    Assign the final Polars DataFrame to result.

    Instructions:
    - Use only the tables given and their columns. Do not make any assumptions about the data, do not use any external data.
    - Respect the data types of the columns. Do not perform any type conversions if not explicitly required by the question.
    - Respect the relationships between the tables. If a question requires joining tables, use the appropriate keys for the join.
    - Respect the semantics of the question. If a question asks for a specific calculation, use the appropriate Polars functions to perform that calculation.
    - Respect the code python syntax. Do not generate any code that would result in a syntax error.
    - The code you produce must be executable without any modifications.
    - The code you produce must be executable without any error. Do not generate any code that would result in an error when executed.

    Available datasets to use for answering the question and generating the Polars code:

[config4]
model_name = Mungert/Llama3.3-8B-Instruct-Thinking-Claude-4.5-Opus-High-Reasoning-GGUF
dtype = torch.float16
device_map = auto
max_new_tokens = 512
do_sample = True
use_cache = True
content_system_prompt = Return only valid Python Polars code. No markdown fences. Assign the final Polars DataFrame to result. Available datasets: